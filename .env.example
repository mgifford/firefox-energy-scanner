# Copy to .env and fill in. NEVER commit .env.
#
# Credentials are read from the environment only. They are never logged,
# never written to benchmark output, and never placed in a URL.

DRUPAL_BASE_URL=https://example.tugboatqa.com
DRUPAL_USERNAME=
DRUPAL_PASSWORD=
