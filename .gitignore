node_modules/
dist/
*.log

# Local scratch output only. Anchored to the repo root so that published
# results under site/results/ are still tracked.
/results/

# Never commit credentials.
.env
.env.local

# Raw Gecko profiles can be very large and may embed page URLs.
*.profile.json
profiles/
